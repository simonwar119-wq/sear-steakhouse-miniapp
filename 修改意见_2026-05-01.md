# SEAR STEAK 会员小程序 · 代码审查与修改意见

> 审查日期：2026-05-01
> 审查范围：全部 6 个前端页面 + 5 个云函数 + 配置文件
> 审查类型：安全漏洞 / 业务逻辑 / 数据完整性 / 运维隐患 / 用户体验

---

## 🔴 一、安全漏洞

### 1. OCR 密钥硬编码在云函数源码中

**位置**：`cloudfunctions/ocrAndAddPoints/index.js` 第 9-10 行
**现状**：
```js
const SECRET_ID='YOUR_T...T_ID'
const SECRET_KEY='YOUR_T..._KEY'
```
即使填了真实密钥，云函数源码在微信云开发控制台明文可读。任何有云开发权限的人都能看到腾讯云 SecretKey，若该密钥还绑了 COS/短信等服务，风险更大。

**修复建议**：改用微信云开发环境变量（高级版支持），或通过腾讯云 API 网关签发临时密钥。

---

### 2. 兑换无核销机制

**位置**：`cloudfunctions/redeemReward/index.js`
**现状**：云函数扣完积分直接返回 `{ success: true }`，没有任何核销码 / 核销状态 / 领取凭证。客户点"兑换"→积分扣了→到前台说"我换了瓶酒"→员工无法核实。

**修复建议**：
- 兑换成功后生成 6 位数字核销码 + 有效期（如 30 天）
- 数据库 `transactions` 增加 `redeemCode`、`redeemedAt`、`redeemedBy` 字段
- 员工后台增加"核销"入口，输入核销码标记已领取
- 客户页面显示核销码 + 有效期倒计时，可截图保存

---

### 3. 管理员 TabBar 全员可见

**位置**：`app.json` tabBar 配置
**现状**：所有用户都能看到「管理后台」tab，点进去虽然无权限但暴露了管理入口。

**修复建议**：移除 TabBar 中的 admin 入口，改为隐藏入口（如长按会员卡 5 秒弹出密码验证，或输入特定口令 `/admin`）。

---

## 🔴 二、业务逻辑漏洞

### 4. 注册赠品"餐后甜品"无核销

**位置**：`cloudfunctions/register/index.js` → `members.registrationGiftUsed: false`
**现状**：注册时标记了 `registrationGiftUsed: false`，但该字段从未在实际消费流程中被核销或检查。客户第一次来可以说"我有注册甜品"→员工给→但系统无已核销记录，同一个客户下次还能说"上次没用"。

**修复建议**：
- 增加核销流程：注册成功后系统生成一张"甜品兑换券"（有效期 7 天）
- 员工后台或有核销码入口，核销后标记 `registrationGiftUsed: true`
- 或改用微信卡券接口，发一张限时优惠券到用户卡包

---

### 5. 防重复上传有 6 小时窗口漏洞

**位置**：`cloudfunctions/ocrAndAddPoints/index.js` → `checkDuplicate()`
**现状**：防重复逻辑是"同一会员 + 相同金额 + 6 小时内"视为重复。如果客户中午消费过，把同一张小票照片**隔 8 小时再传一次**（金额相同，超 6 小时窗口），防重复不生效，可拿双倍积分。

**修复建议**：
- 增加图片文件 MD5 去重（上传时计算 `fileID` 或内容的哈希）
- 同时将金额相同的重复检查窗口从 6 小时放宽到 48 小时（同一天内几乎不可能两次消费完全相同的金额）
- 考虑上传图片的 `fileID` 在云存储中是唯一的，可以基于 `receiptFileID` 做精确去重

---

### 6. 兑换无时效与次数限制

**位置**：`cloudfunctions/redeemReward/index.js`
**现状**：客户可以攒 3 万分一次性兑换 10 瓶酒，餐厅单次亏 10 瓶酒的毛利。无每月限量、无领取时效。

**修复建议**：
- 同一会员每月最多兑换 2 瓶
- 兑换后 30 天内必须到店领取，过期核销码失效、积分退回
- 数据库增加 `rewardMonthlyLimit` 字段追踪当月已兑换次数

---

## 🟡 三、数据完整性问题

### 7. 生日可选无效日期（如 2 月 30 日）

**位置**：`pages/register/register.js`
**现状**：天数 picker 硬编码 1-31 日，不根据月份动态变化。

**修复建议**：
```js
function getDaysInMonth(month) {
  return new Date(2024, month, 0).getDate() // 利用 Date 自动计算
}
```
选择月份后动态更新天数列表。

---

### 8. 手机号获取失败回退到"未知号码"

**位置**：`cloudfunctions/register/index.js` 第 22-27 行
**现状**：
```js
phone = phoneCode.startsWith('test_') ? phoneCode.replace('test_', '') : '未知号码'
```
如果 `getPhoneNumber` API 调用失败（网络/权限），回退显示"未知号码"。管理员在后台按手机号查人时完全找不到。

**修复建议**：
- 手机号获取失败时提示用户重新授权，不偷偷 fallback
- 删除开发测试遗留代码（`test_` 前缀处理）
- 管理员后台增加"按姓名查找"或"按 openid 查找"入口作为备选

---

### 9. 积分过期清零后 `lastActivityDate` 未更新

**位置**：`cloudfunctions/getMember/index.js` → `checkPointsExpiry()`
**现状**：积分清零后 `lastActivityDate` 仍为 1 年多前的旧值，下次检查可能触发重复清零写入，虽不影响业务但会产生垃圾操作记录。

**修复建议**：清零后更新 `lastActivityDate` 为当前时间。

---

## 🟡 四、运维与部署隐患

### 10. 废弃的 `config.js` 包含不同的 ENV_ID

**位置**：`cloudfunctions/config.js`
**现状**：
- `app.js` ENV_ID：`cloud1-d6gc0lkxc53e45276`
- `config.js` ENV_ID：`28370518bcb0367d0921a4a0a0e7e800`
- 云函数实际用 `cloud.DYNAMIC_CURRENT_ENV`，`config.js` 无人引用

**修复建议**：删除 `cloudfunctions/config.js`，统一使用动态环境变量。

---

### 11. OCR 金额解析正则可能不匹配餐厅小票

**位置**：`cloudfunctions/ocrAndAddPoints/index.js` → `parseAmount()`
**现状**：硬编码了"实付"、"合计"、"总计"等关键词。餐厅实际小票格式（尤其是手撕票/美团收银/微信商户小票）可能用"应收"、"网付"、"微信支付"、"小计"等不同词汇。

**修复建议**：发布前拿 5-10 张牛排馆真实小票拍照测试，根据实际格式补充正则模式。常见小票格式举例：
- `微信支付 ¥268.00`
- `应收 ¥368.00`
- `小计 ¥158.00`
- `合计：¥198.00`

---

## 🟢 五、用户体验问题

### 12. 上传小票处理中无进度反馈

**位置**：`pages/upload/upload.js` + `upload.wxml`
**现状**：OCR 调用耗时 3-10 秒，全程只有文字"识别中，请稍候..."，无动画或进度指示器。

**修复建议**：增加步骤提示（上传中 → 识别中 → 积分处理中）+ 转圈动画。

---

### 13. 兑换成功后无客户凭证

**位置**：`pages/rewards/rewards.js`
**现状**：兑换成功只弹 Toast"兑换成功！请告知服务员"，客户到前台无法自证。

**修复建议**：兑换成功后显示大号核销码 + 商品名称 + 有效期，支持截图保存。

---

### 14. 未注册用户切换 Tab 看到空白页

**位置**：`pages/upload/upload.js`、`pages/rewards/rewards.js`
**现状**：未注册用户点"上传小票"能进入但无意义，点"积分兑换"显示空白。

**修复建议**：upload 和 rewards 页面在 `onShow` 中检测登录状态，未注册时引导到注册页。

---

### 15. 废弃文件 `i18n/base.json`

**位置**：`i18n/base.json`
**现状**：项目中存在 i18n 目录但无任何国际化代码引用该文件。

**修复建议**：删除或确认是否未来有计划做国际化。

---

## 📋 修复优先级总表

| 优先级 | 问题编号 | 问题简述 | 影响 |
|--------|---------|---------|------|
| **P0 必修** | #2 | 兑换无核销码 | 客户与餐厅纠纷，诚信漏洞 |
| **P0 必修** | #4 | 注册赠品无核销 | 免费送甜品但无法确认已核销 |
| **P0 必修** | #5 | 防重复上传窗口漏洞 | 可刷双倍积分 |
| **P1 建议修** | #1 | OCR 密钥硬编码 | 密钥泄露风险 |
| **P1 建议修** | #7 | 生日可选无效日期 | 数据脏数据 |
| **P1 建议修** | #8 | 手机号获取失败 fallback | 会员查不到 |
| **P1 建议修** | #10 | 废弃 config.js | 部署混淆 |
| **P2 可优化** | #3 | 管理员入口暴露 | 体验问题 |
| **P2 可优化** | #6 | 兑换无时效次数限制 | 集中兑换风险 |
| **P2 可优化** | #9 | 过期清零后 `lastActivityDate` | 多余写入 |
| **P2 可优化** | #11 | OCR 正则不健壮 | 识别成功率 |
| **P3 锦上添花** | #12-15 | UX 细节 | 用户体验 |

---

*本文件由 Hermes Agent 自动生成，供后续开发参考。*
